import { AssetProvider } from "components/AssetProvider/AssetProvider"
import { Card } from "components/Card/Card"

type EpochsKeyInformationArgs = {
  assetId: string
}

export const EpochsKeyInformation: React.FC<EpochsKeyInformationArgs> = ({
  assetId
}) => {
  return null
}